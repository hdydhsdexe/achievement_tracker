Text display robustness update / 文字显示鲁棒性更新 — v0.7.2

- Added native 8/10/12px F3 bitmap fonts with texture smoothing disabled.
- Reflowed full bilingual conditions and dynamically sized F3 pages for the active resolution.
- Separated HUD and F3 text settings; HUD now auto-fits and stays inside safe bounds.
- Migrated saved settings to schema 6 without overwriting position preferences.

- 新增关闭纹理平滑的 F3 原生 8/10/12px 位图字库。
- 完整换行中英成就条件，并根据当前分辨率动态调整 F3 分页。
- 分离 HUD 与 F3 字号设置；HUD 会自动缩小并保持在安全区域内。
- 存档升级到 schema 6，保留用户原始位置偏好。
